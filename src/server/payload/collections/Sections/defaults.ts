/**
 * Sections content defaults — reuses the exercise content schema + factory.
 *
 * Sections are sub-units of an exercise and store the same block-stream
 * shape as exercises (question_* + rich_text blocks), so we just forward
 * the exercise defaults.
 */

export { DEFAULT_CONTENT } from '../Exercises/defaults'
