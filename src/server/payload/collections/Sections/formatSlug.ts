/**
 * Re-export shared formatSlug utility for Sections hooks.
 * Mirrors Exercises/formatSlug.ts so the hook file imports stay local.
 */
export { formatSlug } from '@/server/payload/fields/formatSlug'
